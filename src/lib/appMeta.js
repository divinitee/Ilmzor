// Single switch for every beta marker in the product (BETA pills, the landing
// beta notice, the "Founder price" label). Flip this to false when VIRORA
// leaves beta and all of it disappears — nothing else should hardcode it.
export const IS_BETA = true;